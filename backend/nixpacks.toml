[phases.setup]
nixPkgs = ["python39Full"]

[phases.install]
commands = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt"
]

[start]
command = "sh -c 'uvicorn server:app --host 0.0.0.0 --port ${PORT}'"
